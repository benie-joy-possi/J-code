[package]
name = "jet-tui"
version.workspace = true
edition.workspace = true

[features]
# Enable real microphone capture for voice PTT mode.
voice = ["jet-core/voice", "dep:cpal", "dep:hound"]

# UI & Interaction features (pass-through from cc-core)
token_budget = ["jet-core/token_budget"]
ultraplan = ["jet-core/ultraplan"]
ultrathink = ["jet-core/ultrathink"]
history_picker = ["jet-core/history_picker"]
message_actions = ["jet-core/message_actions"]
quick_search = ["jet-core/quick_search"]
away_summary = ["jet-core/away_summary"]
hook_prompts = ["jet-core/hook_prompts"]
kairos_brief = ["jet-core/kairos_brief"]
kairos_channels = ["jet-core/kairos_channels"]
lodestone = ["jet-core/lodestone"]

# Agents & Memory features (pass-through from cc-core)
agent_triggers = ["jet-core/agent_triggers"]
agent_triggers_remote = ["jet-core/agent_triggers_remote"]
extract_memories = ["jet-core/extract_memories"]
verification_agent = ["jet-core/verification_agent"]
builtin_explore_plan_agents = ["jet-core/builtin_explore_plan_agents"]
cached_microcompact = ["jet-core/cached_microcompact"]
compaction_reminders = ["jet-core/compaction_reminders"]
agent_memory_snapshot = ["jet-core/agent_memory_snapshot"]
teammem = ["jet-core/teammem"]

# Tools & Infrastructure features (pass-through from cc-core)
bash_classifier = ["jet-core/bash_classifier"]
bridge_mode = ["jet-core/bridge_mode"]
mcp_rich_output = ["jet-core/mcp_rich_output"]
connector_text = ["jet-core/connector_text"]
unattended_retry = ["jet-core/unattended_retry"]
new_init = ["jet-core/new_init"]
powershell_auto_mode = ["jet-core/powershell_auto_mode"]
shot_stats = ["jet-core/shot_stats"]
tree_sitter_bash = ["jet-core/tree_sitter_bash"]
tree_sitter_bash_shadow = ["jet-core/tree_sitter_bash_shadow"]
native_clipboard_image = ["jet-core/native_clipboard_image"]
prompt_cache_break_detection = ["jet-core/prompt_cache_break_detection"]
ccr_auto_connect = ["jet-core/ccr_auto_connect"]
ccr_mirror = ["jet-core/ccr_mirror"]
ccr_remote_setup = ["jet-core/ccr_remote_setup"]

[dependencies]
jet-core = { workspace = true }
jet-api = { workspace = true }
jet-tools = { workspace = true }
jet-query = { workspace = true }
jet-mcp = { workspace = true }
tokio = { workspace = true }
futures = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
tracing = { workspace = true }
ratatui = { workspace = true }
crossterm = { workspace = true }
unicode-width = { workspace = true }
chrono = { workspace = true }
parking_lot = { workspace = true }
dirs = { workspace = true }
similar = { workspace = true }
syntect = { version = "5", default-features = false, features = ["default-syntaxes", "default-themes", "regex-fancy"] }
once_cell = { workspace = true }
base64 = { workspace = true }
reqwest = { workspace = true }
dashmap = { workspace = true }
icy_sixel = { workspace = true }
image = { workspace = true }
regex = { workspace = true }
# Optional audio capture for voice PTT (gated behind the `voice` feature)
cpal = { version = "0.15", optional = true }
hound = { version = "3.5", optional = true }

[dev-dependencies]
tempfile = { workspace = true }
